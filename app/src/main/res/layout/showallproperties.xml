<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/parentShowAllProperty"
    app:layout_behavior="@string/appbar_scrolling_view_behavior">

    <android.support.v7.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/colorPrimary"
        android:minHeight="?attr/actionBarSize"
        android:titleTextColor="#FDB415"
        android:theme="@style/AppTheme"
        android:popupTheme="@style/ThemeOverlay.AppCompat.Dark"
        android:subtitleTextColor="#FDB415"
        android:elevation="10dp"
        >

    </android.support.v7.widget.Toolbar>

    <ListView
        android:id="@+id/listViewShowAllProperties"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:divider="@color/yellow"
        android:dividerHeight="2dp"
        android:padding="10dp"
        android:layout_marginTop="60dp"
        android:layout_below="@+id/toolbar"
         >
    </ListView>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="You donot Have any property Yet Please add"
        android:id="@+id/textviewEmtpyProeprties"
        android:layout_gravity="center"
        />




</FrameLayout>
